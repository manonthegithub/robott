# Progress Log — Robot HTTP Control API

## Phase status
- [x] Spec — approved (`docs/spec.md`)
- [x] Architecture — approved (`docs/architecture.md`)
- [x] Plan — approved (`docs/plan.md`)
- [x] Component 1: `internal/command` — implemented + unit tests written
- [x] Component 2: `internal/hardware` — interfaces implemented (compile-only, no logic)
- [x] Component 3: `internal/hardware/gpiodirect` — implemented + unit tests written (fake line / fake sysfs dir, no real hardware needed)
- [x] Component 4: `internal/executor` — implemented + unit tests written
- [x] Component 5: `internal/api` — implemented + unit tests written (httptest)
- [x] Component 6: `internal/config` — implemented + unit tests written
- [x] Component 7: `cmd/robottt/main.go` — wiring implemented
- [x] **Built + tested on Pi 5** (Go 1.22.12, arm64) — `go test ./...` all green: `command`, `api`, `config`, `executor`, `gpiodirect` pass; `cmd/robottt` and `hardware` correctly have no test files (wiring / pure interfaces).
- [x] LED confirmed working end-to-end on real hardware (`GPIO_CHIP=gpiochip0`, `LED_OFFSET=17`); stepper/servo temporarily disabled in `main.go` pending wiring (see executor nil-guard).
- [x] Component 8: OpenAPI spec + codegen HTTP layer (`docs/plan.md` §8) — motivated by the robot being LLM/MCP-controlled, so a formal API contract is needed. `openapi.yaml` written at repo root; `internal/api` rewritten to implement generated `apigen.StrictServerInterface` (`internal/api/gen`, driven by `//go:generate` + `oapi-codegen.yaml`); `dto.go` deleted (types now generated); `router.go` rewired onto the generated strict handler with a custom error formatter (keeps `{"error": "..."}` shape) and a body-size-limit middleware (generator doesn't apply one). Tests rewritten (still black-box httptest, mostly unchanged since HTTP contract didn't change) plus a new 500-path test.
- [x] **Component 8 verified on Pi 5** — `go generate ./... && go mod tidy && go build ./... && go test ./...` all green. One round of fixes needed: oapi-codegen v2 hoists shared `components.responses` into named types (`QueuedJSONResponse`, `BadRequestJSONResponse`, `QueueFullJSONResponse`, `InternalErrorJSONResponse`) embedded anonymously in each operation's response type, rather than aliasing the schema type directly — response construction in `handlers.go` fixed to go through the embedded field.
- [x] Component 9: serve OpenAPI spec over HTTP (`docs/plan.md` §9) — moved `openapi.yaml` into `internal/api/` (go:embed can't reach outside its package dir), embedded via `spec.go`, served at `GET /openapi.yaml`. Updated the `//go:generate` path in `internal/api/gen/generate.go` to match (`../openapi.yaml`, was `../../../openapi.yaml`). Router now built via `apigen.HandlerFromMux` so this extra route shares the mux with the generated one.
- [x] Component 10: MCP wrapper (`docs/plan.md` §10) — `internal/mcpserver` wraps a generated `apigen.ClientWithResponses` (client generator enabled in `oapi-codegen.yaml`), exposes `set_led`/`move_stepper`/`set_servo` as MCP tools via `github.com/modelcontextprotocol/go-sdk`.
- [x] **Components 9+10 verified on Pi 5** — `go generate ./... && go mod tidy && go build ./... && go test ./...` all green, first try, no fixes needed. `HandlerFromMux`, the `modelcontextprotocol/go-sdk` API surface, and the `apigen.StepperRequestDir` enum type all matched what was written.
- [x] Switched `cmd/robottt-mcp` transport from stdio to HTTP (`mcp.NewStreamableHTTPHandler`) — stdio meant Claude Code (on a separate dev machine) would need to SSH into the Pi to spawn the binary as a subprocess.
- [x] **Folded `cmd/robottt-mcp` into `cmd/robottt` entirely** (still not yet rebuilt on the Pi) — questioned whether 2 processes/ports was actually buying anything for one robot on one Pi; it wasn't, just config to keep in sync (`ROBOT_API_URL`, `MCP_LISTEN_ADDR`). Now: `internal/mcpserver.Server` wraps `*api.Handlers` directly (no generated client, no HTTP round-trip to itself — response objects rendered via `httptest.ResponseRecorder` against their `Visit*Response` method), `cmd/robottt-mcp` deleted, `cmd/robottt/main.go` mounts REST at `/` and MCP at `/mcp` on one `http.ServeMux`/port. `client: true` removed from `oapi-codegen.yaml` again (no longer needed). **Needs `go generate ./... && go mod tidy && go build ./... && go test ./...` on the Pi** — unverified this round: `mcp.NewStreamableHTTPHandler`'s exact signature (carried over from the previous unverified round, since it was never rebuilt after the stdio→HTTP switch).
- [x] Architecture + plan updated for delayed/sequenced commands (`docs/architecture.md`, `docs/plan.md` §11-15) — `delay_ms` on the 3 existing endpoints, new `POST /sequence`/`POST /sequence/stop`, `internal/sequence` package with an `Operation`/`HardwareCommand`/`Loop` hierarchy independent of `internal/command`. The wire schema for a sequence step is a real discriminated `oneOf` (`LedOperation`/`ServoOperation`/`StepperOperation`/`LoopOperation` behind an `Operation` union, matching the class hierarchy discussed from the start) — an earlier draft hedged this to a flat tagged struct over oapi-codegen `oneOf`-support risk, reverted per explicit direction. Risk still real (unverifiable without a local toolchain) but now isolated to one mapper (`internal/api/sequence_convert.go`, written against oapi-codegen's documented `Discriminator()`/`As<Variant>()` pattern for discriminated unions) rather than avoided by changing the wire design.
- [x] Component 11: `internal/command` — `EnqueueBlocking` (blocks on full channel, `ctx` only for cancellation, checks `ctx.Err()` upfront so an already-cancelled `ctx` never sends even with free capacity) + `DelayedEnqueue` helper (sleep then blocking-enqueue; negative delay treated as zero, no hang). Unit tests written covering: immediate return with free capacity, blocks then unblocks on space freeing, blocks then unblocks on `ctx` cancel, already-cancelled `ctx` never sends despite free capacity, zero/negative/positive delay timing, `ctx` cancel during delay vs during the blocked send. **Not yet built/tested on Pi** (no local Go toolchain).
- [x] Component 12: `internal/sequence` — `Operation`/`HardwareCommand`/`Loop`/`OperationSequence` types (no import of `internal/command`), `toCommand` converter (panics on unrecognized type — defensive, unreachable from validated input), `validate` (unknown-type/negative-times/empty-loop-body/negative-delay/bad-stepper-dir/max-depth-5 checks). Tests: happy path matching the architecture doc's example, every rejection case table-driven, empty-top-level-seq-is-valid, `Times==1` boundary, exact `MaxDepth`/`MaxDepth+1` boundary, triple-nested finite loops, `toCommand` per-type mapping + panic-on-unknown-type.
- [x] Component 13: `internal/sequence` — `Sequencer` (`Start`/`Stop`/`exec`), single-slot concurrency (`running`/`cancel` under one mutex — a `Start()` right after a `Stop()` correctly blocks until the cancelled goroutine actually finishes, since `running` only flips false inside `run()` after `exec()` returns, not inside `Stop()` itself). Tests: nested-example ordering via a draining fake queue, exact count for a finite loop, infinite loop stopped promptly (no further enqueues after `Stop()`), `Start()` while running → `ErrAlreadyRunning`, `Stop()` with nothing running → `ErrNotRunning`, a sequence blocked on a full queue is unblocked by `Stop()` (doesn't hang), restart immediately after natural completion succeeds, a rejected (invalid) `Start()` doesn't leave `running` true.
- [x] Component 14: `delay_ms` on `/led`/`/stepper`/`/servo` + new `/sequence`/`/sequence/stop` — `openapi.yaml` updated: `LedRequest`/`StepperRequest`/`ServoRequest` renamed outright to `LedOperation`/`StepperOperation`/`ServoOperation` (no `allOf` wrapper, no separate `*Request` schema alongside — the standalone endpoints and sequence leaves share literally the same schema, `type` and `delay_ms` added directly onto each), plus `LoopOperation`, unified as a discriminated `oneOf` `Operation`; `SequenceRequest{seq: Operation[]}`; new `Conflict`/`NotFound`/`Stopped` responses. `type` is schema-required on all 3 command operations now (proper discriminator practice) but, like every other JSON Schema constraint in this spec, documentation-only — not enforced by a validation middleware, so existing standalone callers that omit `type` are unaffected. `internal/mcpserver/server.go` updated for the renamed types (`apigen.LedOperation`/`StepperOperation`/`ServoOperation`, `apigen.StepperOperationDir`). `internal/api/handlers.go`: `PostLed`/`PostStepper`/`PostServo` branch on `delay_ms` (0/omitted → unchanged synchronous path; `>0` → `202` immediately + a goroutine on `h.Ctx`, a server-lifetime context now threaded through `Handlers`); new `PostSequence`/`PostSequenceStop` on a new `Sequencer` field. New `internal/api/sequence_convert.go`: `toOperation` calls the generated `Operation.Discriminator()` then the matching `As<Variant>Operation()` accessor, recursively mapping into the `sequence.Operation` hierarchy, applying the exact same servo-range/stepper-steps/stepper-dir checks the standalone endpoints use (dedup via `command.Direction(...)` cast, same pattern already in `PostStepper`). `handlers_test.go` rewritten: thread-safe `fakeQueue` (needed once background goroutines could enqueue concurrently with the test), regression tests for `delay_ms==0`/omitted, a `delay_ms>0` timing test, `/sequence` happy path + servo-range-inside-a-loop rejection + unknown-discriminator rejection + `409` while running, `/sequence/stop` `404`/`202` + no-further-enqueues-after-stop, and a documentation-test for `on` omitted decoding to `false` (a consequence of `allOf`-merged fields rather than a nested sub-object).
- [x] Component 15: `cmd/robottt/main.go` — constructs one `*sequence.Sequencer{Queue: queue}`, moved `signal.NotifyContext` construction earlier so its `ctx` can be threaded into `api.Handlers.Ctx` (needed by the new delayed-enqueue goroutines). No new env vars.
- [x] **Codegen risk flagged, not yet resolved:** `internal/api/gen/server.gen.go` isn't present in this checkout at all (a `go:generate` output, not committed here), so none of components 14-15's generated-type assumptions could be checked against real oapi-codegen v2.4.1 output. Highest-risk spot is the discriminated `oneOf` → `Operation` union type (written against oapi-codegen's documented `Discriminator()`/`As<Variant>Operation()`/`json.RawMessage` pattern, confined entirely to `sequence_convert.go`); lower risk for `delay_ms *int` (plain optional-scalar-to-pointer, same convention as every other optional field in this spec). Expect a fix-up round on the Pi once `go generate ./... && go build ./... && go test ./...` actually runs — same pattern as components 8 and 9/10, which each needed one such round.
- [x] **Post-implementation review of components 11-15** caught two real issues (first spotted by user, second found reviewing `internal/sequence`/`cmd/robottt/main.go` against `docs/architecture.md`):
  1. `internal/mcpserver` hadn't been updated at all for `delay_ms`/`/sequence`/`/sequence/stop` — an LLM driving the robot via MCP (the actual point of this project's pivot) couldn't use any of it, only the original 3 immediate actions.
  2. `sequence.Sequencer` wasn't shutdown-aware: `Start()` always derived its cancellable context from `context.Background()`, never from anything tied to server shutdown, unlike the `delay_ms>0` single-shot path which correctly uses `Handlers.Ctx`. A sequence running during shutdown would've been left permanently parked on `EnqueueBlocking` once the executor stopped draining the queue (leaked goroutine, and an infinite-loop sequence with no way left to ever stop).
  - **Fix for #2:** `Sequencer` gained a `Ctx context.Context` field; `Start()` derives its per-run cancellable context from `s.Ctx` (falling back to `context.Background()` if nil, so existing tests/callers that don't set it are unaffected). `cmd/robottt/main.go` now passes its shutdown `ctx` into the `Sequencer` it constructs. New tests: a running sequence ends on its own when `Ctx` is cancelled (not just via `Stop()`), and the nil-`Ctx` fallback still behaves as before.
  - **Fix for #1:** `internal/mcpserver`'s `LedInput`/`StepperInput`/`ServoInput` gained a `delay_ms` field, passed straight through to the corresponding `apigen.*Operation.DelayMs`. Two new MCP tools, `run_sequence`/`stop_sequence`, mirroring `/sequence`/`/sequence/stop`. New `internal/mcpserver/sequence_convert.go`: `SequenceStepInput` converted via `toGenOperation` into the generated `apigen.Operation` union using its `From<Variant>Operation()` setters — same codegen-risk profile as `internal/api/sequence_convert.go`'s reverse-direction `As<Variant>Operation()` usage, now the *second* place relying on that pattern. New `internal/mcpserver/server_test.go` (package had no tests before): happy paths for all 5 tools, `delay_ms>0` timing, validation-failure-is-an-`IsError`-result (not a Go error) for a bad stepper request, servo-out-of-range-inside-a-sequence rejected, unknown step type surfaces as a Go error (fails before `PostSequence` is even called), and running→stop→no-further-enqueues.
- [x] **Build/runtime fixes found actually running on the Pi:**
  1. `internal/executor/executor_test.go` had its own third `fakeQueue` implementing `CommandQueue` that was missed when `EnqueueBlocking` was added to the interface (`internal/api`'s and `internal/mcpserver`'s were already updated) — build failure, fixed by adding the method.
  2. **Runtime panic** on startup: `AddTool: tool "run_sequence": ... cycle detected for type mcpserver.SequenceStepInput`. The MCP SDK's schema reflector walks a tool input type to build its JSON schema and can't handle a struct that transitively contains itself — `SequenceStepInput.Body []SequenceStepInput` is exactly that. Fixed by changing `Body` to `[]any`; recursion is now handled at runtime instead of at the Go-type level: `decodeStep` re-marshals/unmarshals each element (a `map[string]any` in the real MCP-call case, JSON-decoded generically) back into a concrete `SequenceStepInput` before `toGenOperation` recurses into it. New test (`TestHandleRunSequence_BodyAsRealJSONDecodedMap`) exercises the actual `map[string]any` shape, not just the convenience struct-literal shape the other tests use.
- [x] Component 17: `Par` (fork/join) — added after using `run_sequence` live on the Pi surfaced that a heartbeat blink + servo sweep in one sequence always ran strictly one after the other. `internal/sequence`: `Par{Branches [][]Operation}`, `validate` rejects zero branches (`ErrEmptyPar`) or any empty branch (`ErrEmptyParBranch`), branches count toward `MaxDepth` like a `Loop` body; `Sequencer.exec`'s `Par` case (`execPar`) spawns one goroutine per branch against the shared `Queue`, `sync.WaitGroup`s them, returns the first non-nil error. `openapi.yaml`: `ParOperation{type: "par", branches: Operation[][]}` added to `Operation`'s `oneOf`. `internal/api/sequence_convert.go` and `internal/mcpserver/sequence_convert.go` both gained a `"par"` case (mcpserver's `SequenceStepInput.Branches` is `[][]any`, not `[][]SequenceStepInput` — same schema-reflection-cycle avoidance as `Body`). Tests added at all 3 layers, including a concurrency proof (two branches with different delays enqueue in delay order, not list order) and a join proof (a step after `Par` waits for the slowest branch, not the fastest). **Explicitly flagged, not built:** branches still serialize through the one shared `CommandQueue`/executor (architecture decision #7) — this makes *pacing* independent per branch, not true simultaneous hardware dispatch; that would need per-device executor workers (decision #2's existing revisit), a separate, bigger change only worth it for sub-ms synchronization.
- [ ] Refactor + tooling — not started (linter/formatter, pre-commit hook)
- [ ] Production build — not started
- [ ] Deployment — not started (n/a unless containerized/deployed as a service)
- [ ] Security review — not started

## Decision log
- Chose in-process Go channel (not NATS/Redis) for v1 command queue — hobby scale, no persistence need yet; wrapped behind `CommandQueue` interface for later swap.
- Chose `warthog618/go-gpiocdev` for digital GPIO — only maintained Go lib matching Pi5 RP1 chardev chip.
- Chose hand-rolled sysfs PWM wrapper for servo (not a library) — Pi5 HW PWM Go lib support unconfirmed; sysfs interface is a few files, cheap to hand-roll.
- Chose software GPIO toggle loop for stepper STEP pulses, not HW PWM — HW PWM guarantees frequency/duty but not exact pulse *count*; exact step count matters more for position accuracy. Reversed an earlier verbal suggestion (HW PWM for STEP too) once this was thought through in architecture phase.
- Single serial executor goroutine for all 3 device types — simplest, free hardware mutex, accepted tradeoff that a long stepper move blocks a queued LED toggle.
- Spec requires hardware backend (direct-Go vs MCU co-processor) be swappable behind the same 3 interfaces — no MCU impl built yet (non-goal for v1), but `gpiodirect` package boundary was designed so a future `mcu` package is a drop-in.
- No auth in v1; `api.NewRouter` takes variadic `Middleware`, empty for now — auth becomes one added middleware, no handler rewrite.
- Pivoted to OpenAPI-spec-first + codegen for the HTTP layer once the real goal (LLM/MCP control of the robot) came up — a formal contract is what an MCP wrapper consumes; hand-maintained docs would drift. Retrofit approach chosen over "generate stub then implement" since 3 endpoints were already hand-written and tested — codegen replaces the internals, not the whole component.
- JSON Schema constraints (`dir` enum, `steps` minimum) stay documentation-only; enum/range validation is still hand-written Go in `internal/api/handlers.go`, not enforced by a request-validation middleware (would need the spec kept in lock-step with validation logic twice — revisit only if the schema grows enough that drift becomes a real risk).
- Delayed/sequenced commands (`docs/plan.md` §11-15): the executor never sleeps — `command.DelayedEnqueue` is the single shared sleep site, used both by `delay_ms>0` single-shot requests and by the `sequence.Sequencer`. `EnqueueBlocking`'s blocking comes from the underlying Go channel for free; its `ctx` parameter exists only so `Sequencer.Stop()` can interrupt a sequence parked on a full queue, not to implement the blocking itself.
- `sequence.Operation`/`Loop`/`OperationSequence` are deliberately independent of `internal/command` (no import) — `internal/sequence/convert.go`'s `toCommand` is the only bridge. Chosen so the operation tree stays reusable/testable without pulling in hardware-dispatch types, mirroring the existing DTO→`command.Command` separation in `handlers.go`.
- Only one sequence may run at a time (`Sequencer` is a single slot, not a registry) — second `POST /sequence` gets `409`. Simplest option matching what was asked; "queue the next sequence" would need a wait-queue, scoped separately if ever wanted.
- The wire schema for a sequence step (`Operation`) is a real discriminated `oneOf` (`LedOperation`/`ServoOperation`/`StepperOperation`/`LoopOperation`), matching the internal `Operation`/`Loop` class hierarchy rather than a flattened compromise — an earlier draft avoided this over oapi-codegen `oneOf`-support risk (unverifiable without a local Go toolchain), reverted per explicit direction. The risk is real but now confined to one file, `internal/api/sequence_convert.go`.
- `LedOperation`/`ServoOperation`/`StepperOperation` replace `LedRequest`/`ServoRequest`/`StepperRequest` outright rather than wrapping them via `allOf` — one schema serves both the standalone endpoint and the sequence leaf, not two nearly-identical schemas glued together. An intermediate draft used `allOf` (existing request schema + a `type` field); reverted per explicit direction to a straight rename with `type`/`delay_ms` added directly.
- MCP tool exposure for `delay_ms`/`/sequence`/`/sequence/stop` was initially flagged as out of scope, then built in the post-implementation review pass once the gap was pointed out — see phase-status notes above.

## Open questions / blockers
- Sysfs PWM chip/channel path (`SERVO_CHIP_PATH`, e.g. `/sys/class/pwm/pwmchip0`) depends on which PWM-capable GPIO pin is wired and whether the `dtoverlay=pwm` (or similar) is enabled in Pi5's `/boot/firmware/config.txt` — needs to be set up on the Pi and confirmed before servo will work (architecture decision #3). Stepper STEP/DIR offsets also still need to be confirmed once wired.
- No linter/formatter wired yet (workflow phase 7, not started).
- Run `go generate ./... && go mod tidy && go build ./... && go test ./...` on the Pi for the MCP merge (see phase status above).
- `MCP_LISTEN_ADDR`/`ROBOT_API_URL` env vars no longer apply — MCP now shares `cmd/robottt`'s `LISTEN_ADDR`/`8080`, reachable at `/mcp`.
